<h1>Edit Person</h1>
{% if error is defined and error %}<div>{{ error }}</div>{% endif %}
{% if message is defined and message %}<div>{{ message }}</div>{% endif %}
{{ form_start(form) }}
{{ form_widget(form) }}
<button type="submit">Save</button>
{{ form_end(form) }}
